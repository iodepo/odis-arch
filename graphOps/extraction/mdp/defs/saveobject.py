

def saveObject():
    print("to be done")

    # Save to file
    # print("Saving results to file")
    # _, file_extension = os.path.splitext(o)
    #
    # if file_extension == '.parquet':
    #     mf.to_parquet(o, engine='fastparquet')  # engine must be one of 'pyarrow', 'fastparquet'
    # elif file_extension == '.csv':
    #     mf.to_csv(o)
    # else:
    #     print(f'Error: unsupported file extension {file_extension}. Support .parquet or .csv only')
    #     sys.exit(1)